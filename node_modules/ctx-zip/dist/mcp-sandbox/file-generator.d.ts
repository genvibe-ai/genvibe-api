import type { SandboxProvider } from "./sandbox-provider.js";
import type { MCPServerConfig, ServerToolsMap, ToolDefinition } from "./types.js";
/**
 * Generate a single tool file
 */
export declare function generateToolFile(tool: ToolDefinition, serverName: string): string;
/**
 * Generate server index file that exports all tools
 */
export declare function generateServerIndex(tools: ToolDefinition[], serverName: string): string;
/**
 * Generate the MCP client router that connects to all servers
 */
export declare function generateMCPClient(servers: MCPServerConfig[]): string;
/**
 * Write all files to the sandbox
 */
export declare function writeFilesToSandbox(sandboxProvider: SandboxProvider, serverToolsMap: ServerToolsMap, servers: MCPServerConfig[], outputDir: string): Promise<void>;
