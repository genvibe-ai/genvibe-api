import { Sandbox } from "@vercel/sandbox";
import type { CommandResult, SandboxCommand, SandboxFile, SandboxProvider, SandboxProviderOptions } from "./sandbox-provider.js";
/**
 * Vercel-specific sandbox options
 */
export interface VercelSandboxOptions extends SandboxProviderOptions {
    runtime?: "node22" | "python3.13";
    vcpus?: number;
}
/**
 * Vercel Sandbox provider implementation
 */
export declare class VercelSandboxProvider implements SandboxProvider {
    private sandbox;
    private workspacePath;
    private constructor();
    /**
     * Create a new Vercel sandbox instance
     */
    static create(options?: VercelSandboxOptions): Promise<VercelSandboxProvider>;
    writeFiles(files: SandboxFile[]): Promise<void>;
    runCommand(command: SandboxCommand): Promise<CommandResult>;
    stop(): Promise<void>;
    getId(): string;
    getWorkspacePath(): string;
    /**
     * Get the underlying Vercel sandbox instance (for advanced use cases)
     */
    getVercelSandbox(): Sandbox;
}
