import type { ReadStream } from "node:fs";
import type { StorageAdapter, StorageReadParams, StorageWriteParams, StorageWriteResult } from "./types";
export interface VercelBlobStorageOptions {
    prefix?: string;
    access?: "public";
}
export declare class VercelBlobStorageAdapter implements StorageAdapter {
    private prefix;
    private access;
    constructor(options?: VercelBlobStorageOptions);
    resolveKey(name: string): string;
    write(params: StorageWriteParams): Promise<StorageWriteResult>;
    readText(params: StorageReadParams): Promise<string>;
    openReadStream(params: StorageReadParams): Promise<NodeJS.ReadableStream | ReadStream>;
    toString(): string;
}
export declare function vercelBlobUriToOptions(uri: string): VercelBlobStorageOptions;
