import fs from "node:fs";
import type { StorageAdapter, StorageReadParams, StorageWriteParams, StorageWriteResult } from "./types";
export interface FileStorageOptions {
    baseDir: string;
    prefix?: string;
}
export declare class FileStorageAdapter implements StorageAdapter {
    private baseDir;
    private prefix;
    constructor(options: FileStorageOptions);
    resolveKey(name: string): string;
    write(params: StorageWriteParams): Promise<StorageWriteResult>;
    readText(params: StorageReadParams): Promise<string>;
    openReadStream(params: StorageReadParams): Promise<fs.ReadStream>;
    toString(): string;
}
export declare function fileUriToOptions(uri: string): FileStorageOptions;
