import type { StorageAdapter } from "./types";
export interface GrepResultLine {
    lineNumber: number;
    line: string;
}
export declare function grepObject(adapter: StorageAdapter, key: string, pattern: RegExp): Promise<GrepResultLine[]>;
